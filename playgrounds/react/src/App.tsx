import { Tooltip, IconButton, Button } from "@stylish-ui/react";

export default function App() {
  return (
    <div>
      Hello World This is
      <Tooltip />
      <Button />
      <IconButton />
    </div>
  );
}
