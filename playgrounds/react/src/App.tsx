import { Tooltip, IconButton, Box, Button } from "@stylish-ui/react";

export default function App() {
  return (
    <Box px={20}>
      Hello World This is
      <Tooltip />
      <Button />
      <IconButton />
    </Box>
  );
}
